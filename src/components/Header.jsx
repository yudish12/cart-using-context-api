import React from "react";

import Cart from "./Cart";

const Header = () => {
  return (
    <nav className="nav">
      <h2 className="nav-title">UseContext</h2>
      <Cart />
    </nav>
  );
};

export default Header;
